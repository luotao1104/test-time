# RT-001 Plan-Lite

## 1. Technical Approach

Directly modify the string literals in `src/pages/HomePage.jsx`.

## 2. Implementation Steps

1.  Modify `src/pages/HomePage.jsx`:
    - Change `title: '番茄钟'` to `title: '番茄时间'` in `modules` array.
    - Change `title: '番茄钟'` to `title: '番茄时间'` in `startTour` steps.

## 3. Verification

- Manual check: Verify the text on the Home Page card.
- Manual check: Verify the text in the tour popover.
